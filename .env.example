# Google Cloud Project ID (required)
GOOGLE_CLOUD_PROJECT=your-project-id

# Google Cloud Location (optional, defaults to europe-west1)
GOOGLE_CLOUD_LOCATION=europe-west1

# Gemini model (optional, defaults to gemini-2.0-flash)
# Options: gemini-2.0-flash, gemini-1.5-flash, gemini-1.5-pro
GEMINI_MODEL=gemini-2.0-flash

# Max content length in characters (optional, defaults to 100000)
MAX_CONTENT_LENGTH=100000

# Site info for llms.txt header (optional)
SITE_NAME=Your Website Name
# Option 1: Manual description
SITE_DESCRIPTION=Brief description of what this website is about.
# Option 2: Auto-generate description from URL (uses AI to summarize)
# SITE_ABOUT_URL=https://example.com/about

# Sitemap URL for AI reference (optional)
# If set, llms.txt will include a link to sitemap for complete page listing
SITE_SITEMAP_URL=https://example.com/sitemap.xml

# API retry configuration (optional)
# Used for handling 429 RESOURCE_EXHAUSTED errors with exponential backoff
API_MAX_RETRIES=5
API_INITIAL_DELAY=2.0
